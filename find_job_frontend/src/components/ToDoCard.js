import React from 'react'

const ToDoCard = (prop) => {

    /* */

    const modifyToDo = () =>{

        /*use existing props.todo to get some props of todo
        to be modified and add new props */
    }
    return (
        <div>
            
        </div>
    )
}

export default ToDoCard
