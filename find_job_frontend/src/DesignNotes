1. Pass objects to forms and conditionally render
value from passed object or null in fields to reflect
if a new or existing item  linked the form is being handled

2. Have a prop that just tells what text to pass to button. call it 'buttonText'
3. Make signup require firs_name, lastname, username, email and password



when they sign in:

option:

go to my dashboard
see all jobs
search for specific jobs


when they signup
biuld my resume
see all jobs
search for specific jobs 